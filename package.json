{
  "name": "vencore-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/sync-images.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "generate:images": "node scripts/generate-images.mjs",
    "sync:images": "node scripts/sync-images.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.5",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "sharp": "^0.35.0"
  }
}
